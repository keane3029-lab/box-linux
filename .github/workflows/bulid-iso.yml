name: Build Debian ISO

on:
  push:
    branches: [ master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install live-build and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y live-build debootstrap syslinux-utils xorriso squashfs-tools

      - name: Build ISO
        run: |
          sudo lb clean
          sudo lb config
          sudo lb build

      - name: Rename ISO for clarity
        run: |
          mv *.hybrid.iso box-linux.iso || mv live-image-*.iso box-linux.iso || true

      - name: Publish to Releases (Placeholder / Latest)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Box Linux (Latest Build)
          body: "Automated build from main."
          draft: false
          prerelease: false
          make_latest: "true"
          files: |
            *.iso
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
